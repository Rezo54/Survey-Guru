rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Evidence binaries are protected business data. Browser access is denied
    // by default and must flow through authorised server operations or a future
    // narrowly scoped, independently tested exception.
    match /{object=**} {
      allow read, write: if false;
    }
  }
}
